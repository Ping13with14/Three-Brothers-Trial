# 项目架构设计文档

## 文档信息
- **项目名称**: Unity 游戏项目架构分析
- **创建日期**: 2026-04-18
- **文档版本**: 1.0

---

## 目录
1. [项目概述](#1-项目概述)
2. [目录结构](#2-目录结构)
3. [核心系统架构](#3-核心系统架构)
4. [类关系与数据流向](#4-类关系与数据流向)
5. [当前问题分析](#5-当前问题分析)
6. [改进建议](#6-改进建议)
7. [扩展性设计](#7-扩展性设计)

---

## 1. 项目概述

本项目是一个基于 Unity 引擎的游戏项目，采用模块化设计，包含玩家系统、敌人系统、库存系统、任务系统、对话系统和技能树系统等核心模块。

### 技术栈
- **引擎**: Unity
- **编程语言**: C#
- **UI框架**: Unity UI + TextMeshPro
- **设计模式**: 单例模式、事件系统、观察者模式

---

## 2. 目录结构

```
Assets/Scripts/
├── CameraScripts/              # 相机系统
│   └── ConfinerFinder.cs
├── Enemy/                      # 敌人系统
│   ├── Enemy_Combat.cs         # 敌人战斗
│   ├── Enemy_Health.cs         # 敌人生命
│   ├── Enemy_Knockback.cs      # 敌人击退
│   └── Enemy_Movemont.cs       # 敌人移动
├── Inventory & Shop/           # 库存与商店系统
│   ├── InventoryManger.cs      # 库存管理器
│   ├── InventorySlot.cs        # 库存槽位
│   ├── ItemSo.cs              # 物品数据
│   ├── Loot.cs                # 掉落物品
│   ├── Shop/                  # 商店模块
│   └── ItemSos/               # 物品资源
├── NPC Script/                 # NPC系统
│   ├── DialogueScripts/       # 对话脚本
│   ├── DialogueSOs/           # 对话数据
│   ├── NPCState/              # NPC状态机
│   └── LocationSOs/           # 位置数据
├── Player/                    # 玩家系统
│   ├── PlayerMovement.cs      # 玩家移动
│   ├── PlayerCombat.cs        # 玩家战斗
│   ├── PlayerHealth.cs        # 玩家生命
│   ├── StatsManager.cs        # 属性管理
│   └── Arrow.cs              # 弓箭系统
├── QuestSystem/               # 任务系统
│   ├── QuestManager.cs        # 任务管理器
│   ├── QuestSO.cs             # 任务数据
│   └── QuestLogUI.cs          # 任务日志UI
├── SceneChanger/              # 场景切换
├── SkillTree/                 # 技能树系统
│   ├── SkillManger.cs         # 技能管理器
│   ├── SkillTreeManger.cs     # 技能树UI管理
│   └── SkillSo.cs            # 技能数据
├── Tilemap Scripts/           # 瓦片地图
└── GameManager.cs             # 游戏管理器
```

---

## 3. 核心系统架构

### 3.1 单例管理模式

当前项目使用单例模式管理全局状态：

| 单例类 | 职责 |
|--------|------|
| `GameManager` | 全局游戏状态管理，场景持久化 |
| `StatsManager` | 玩家属性管理（伤害、速度、生命） |
| `InventoryManger` | 库存和金币管理 |
| `DialogueManger` | 对话流程控制 |
| `QuestManager` | 任务进度管理 |

### 3.2 事件驱动模式

系统间通过事件进行通信：

```csharp
// 怪物死亡事件
Enemy_Health.OnMonsterDefeated += ExpManager.OnMonsterDefeated;

// 物品拾取事件
Loot.OnItemLooted += InventoryManger.AddItem;

// 技能点消耗事件
SkillSlot.OnAbilityPointSpent += SkillManger.HandleAbilityPointSpent;
```

---

## 4. 类关系与数据流向

### 4.1 核心类图

```
┌─────────────────────────────────────────────────────────────┐
│                        GameManager                          │
│  (单例 - 全局管理)                                           │
│  - DialogueManger                                           │
│  - DialogueHistoryTracker                                   │
│  - LocationHistoryTracker                                   │
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│ StatsManager  │ │InventoryManger│ │ DialogueManger│
│ (单例 - 属性)  │ │ (单例 - 库存)  │ │ (对话管理)     │
└───────┬───────┘ └───────┬───────┘ └───────┬───────┘
        │                │                │
        ▼                ▼                ▼
┌───────────────┐ ┌───────────────┐ ┌───────────────┐
│PlayerMovement │ │  InventorySlot│ │   NPC_Talk    │
│ PlayerCombat  │ │    Loot       │ │      NPC      │
│ PlayerHealth  │ │   UserItem    │ │      NPC_Wander│
└───────┬───────┘ └───────┬───────┘ └───────┬───────┘
        │                │                │
        └────────┬────────┴────────────────┘
                 ▼
        ┌───────────────┐
        │  Enemy_Health │
        │  Enemy_Combat │
        │Enemy_Movement │
        │Enemy_Knockback│
        └───────────────┘
```

### 4.2 数据流向图

```
                    ┌─────────────────┐
                    │   玩家输入       │
                    │  (键盘/手柄)     │
                    └────────┬────────┘
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                     PlayerMovement                          │
│  - 移动处理                                                   │
│  - 方向翻转                                                  │
│  - 击退处理                                                   │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      PlayerCombat                            │
│  - 攻击检测                                                   │
│  - 伤害计算 (StatsManager.damage)                           │
│  - 敌人击退                                                   │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     Enemy_Health                            │
│  - 生命值管理                                                │
│  - 死亡检测                                                  │
│  - 触发 OnMonsterDefeated 事件                              │
└──────────────────────────┬──────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      ExpManager                             │
│  - 经验值计算                                                │
│  - 升级处理                                                  │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 库存系统数据流

```
物品掉落 (Loot)
      │
      ▼
InventoryManger.AddItem()
      │
      ├───[是金币]──▶ gold += quantity
      │
      └───[是物品]──▶ 查找相同物品槽
                        │
                        ├───[有空间]──▶ 堆叠
                        │
                        └───[无空间]──▶ 查找空槽位
                                            │
                                            ├───[有空槽]──▶ 放入
                                            │
                                            └───[无空槽]──▶ DropLoot() 掉落
```

---

## 5. 当前问题分析

### 5.1 代码重复

| 问题类型 | 出现位置 | 重复代码 |
|----------|----------|----------|
| 单例模式 | GameManager, StatsManager, InventoryManger | Awake()中的单例创建逻辑 |
| UI更新 | InventorySlot, QuestLogSlot | UpdateUI()方法 |
| 事件订阅 | 多个类 | OnEnable/OnDisable中的事件订阅 |

### 5.2 紧耦合问题

```csharp
// PlayerCombat.cs 第40-41行 - 直接依赖具体类
enemies[0].GetComponent<Enemy_Health>().ChangeHealth(...);
enemies[0].GetComponent<Enemy_Knockback>().Knockback(...);

// QuestManager.cs 第24行 - 直接依赖InventoryManger
InventoryManger.Instance.GetItemQuantity(objective.targetItem);
```

### 5.3 缺少抽象层

| 场景 | 当前实现 | 问题 |
|------|----------|------|
| 输入处理 | Input.GetButton() | 无法切换输入设备 |
| 伤害系统 | 直接调用 | 无法支持暴击、护甲等机制 |
| 物品使用 | switch-case | 难以扩展新物品类型 |

### 5.4 性能考虑

| 问题 | 影响 | 建议 |
|------|------|------|
| Loot对象频繁Instantiate/Destroy | GC压力 | 对象池 |
| 事件订阅未取消 | 内存泄漏 | 确保OnDisable取消订阅 |
| 频繁查找组件 | 性能开销 | 缓存组件引用 |

---

## 6. 改进建议

### 6.1 单例基类抽取

```csharp
public abstract class Singleton<T> : MonoBehaviour where T : MonoBehaviour
{
    private static T instance;
    public static T Instance
    {
        get
        {
            if (instance == null)
            {
                instance = FindObjectOfType<T>();
            }
            return instance;
        }
    }

    protected virtual void Awake()
    {
        if (instance != null && instance != this)
        {
            Destroy(gameObject);
            return;
        }
        instance = this as T;
        DontDestroyOnLoad(gameObject);
    }
}
```

### 6.2 接口抽象

```csharp
public interface IDamageable
{
    void ChangeHealth(int amount);
    int CurrentHealth { get; }
    int MaxHealth { get; }
}

public interface IKnockbackable
{
    void Knockback(Transform source, float force, float knockbackTime, float stunTime);
}

public interface IAttackable
{
    void TakeDamage(int damage, Transform attacker);
}
```

### 6.3 事件系统统一

```csharp
public static class GameEvents
{
    // 战斗事件
    public static event Action<int> OnMonsterDefeated;
    public static event Action<Transform, int> OnPlayerDamaged;

    // 物品事件
    public static event Action<ItemSo, int> OnItemLooted;
    public static event Action<ItemSo> OnItemUsed;

    // 任务事件
    public static event Action<QuestSO> OnQuestStarted;
    public static event Action<QuestSO> OnQuestCompleted;

    // 通知方法
    public static void MonsterDefeated(int exp) => OnMonsterDefeated?.Invoke(exp);
    public static void ItemLooted(ItemSo item, int quantity) => OnItemLooted?.Invoke(item, quantity);
}
```

### 6.4 配置集中管理

```csharp
[CreateAssetMenu(fileName = "GameConfig", menuName = "Game/GameConfig")]
public class GameConfig : ScriptableObject
{
    [Header("玩家配置")]
    public PlayerConfig player;

    [Header("敌人配置")]
    public EnemyConfig enemy;

    [Header("战斗配置")]
    public CombatConfig combat;

    [Header("库存配置")]
    public InventoryConfig inventory;
}

[System.Serializable]
public class PlayerConfig
{
    public int defaultHealth = 100;
    public int defaultSpeed = 5;
    public int defaultDamage = 10;
}
```

### 6.5 对象池实现

```csharp
public class ObjectPool<T> where T : MonoBehaviour
{
    private Queue<T> availableObjects = new();
    private T prefab;
    private Transform parent;

    public void Initialize(T prefab, int initialSize, Transform parent = null)
    {
        this.prefab = prefab;
        this.parent = parent;

        for (int i = 0; i < initialSize; i++)
        {
            T obj = Object.Instantiate(prefab, parent);
            obj.gameObject.SetActive(false);
            availableObjects.Enqueue(obj);
        }
    }

    public T Get()
    {
        T obj = availableObjects.Count > 0
            ? availableObjects.Dequeue()
            : Object.Instantiate(prefab, parent);

        obj.gameObject.SetActive(true);
        return obj;
    }

    public void Return(T obj)
    {
        obj.gameObject.SetActive(false);
        availableObjects.Enqueue(obj);
    }
}
```

### 6.6 输入抽象

```csharp
public interface IInputProvider
{
    float Horizontal { get; }
    float Vertical { get; }
    bool IsAttackPressed { get; }
    bool IsInteractionPressed { get; }
}

public class KeyboardInputProvider : IInputProvider
{
    public float Horizontal => Input.GetAxis("Horizontal");
    public float Vertical => Input.GetAxis("Vertical");
    public bool IsAttackPressed => Input.GetButtonDown("Slash");
    public bool IsInteractionPressed => Input.GetButtonDown("Interact");
}
```

---

## 7. 扩展性设计

### 7.1 伤害系统扩展

```csharp
public interface IDamageModifier
{
    int CalculateDamage(int baseDamage, DamageContext context);
}

public class DamageContext
{
    public Transform attacker;
    public Transform target;
    public DamageType type;
    public bool isCritical;
}

public enum DamageType
{
    Physical,
    Magic,
    True
}
```

### 7.2 物品系统扩展

```csharp
public interface IItemEffect
{
    void Apply(GameObject target);
    void Remove(GameObject target);
}

public class HealthPotionEffect : IItemEffect
{
    public int healAmount;

    public void Apply(GameObject target)
    {
        target.GetComponent<StatsManager>()?.UpdateHealth(healAmount);
    }

    public void Remove(GameObject target) { }
}
```

### 7.3 技能系统扩展

```csharp
public abstract class Skill
{
    public string skillName;
    public float cooldown;
    public abstract void Execute(Transform caster);
}

public class FireballSkill : Skill
{
    public GameObject projectilePrefab;
    public float speed;

    public override void Execute(Transform caster)
    {
        // 实现火球术逻辑
    }
}
```

---

## 附录：改进优先级

| 优先级 | 改进项 | 工作量 | 收益 |
|--------|--------|--------|------|
| P0 | 单例基类抽取 | 低 | 高 |
| P0 | 事件系统统一 | 中 | 高 |
| P1 | 接口抽象（IDamageable等） | 中 | 高 |
| P1 | 对象池（Loot） | 低 | 中 |
| P2 | 配置集中管理 | 中 | 中 |
| P2 | 输入系统抽象 | 中 | 中 |
| P3 | 依赖注入容器 | 高 | 中 |

---

## 修订历史

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| 1.0 | 2026-04-18 | 初始文档创建 |

---

*本文档由代码分析工具自动生成*
